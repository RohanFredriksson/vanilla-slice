// @slice/angular — placeholder module.
// Framework INTEGRATION. Implementation (canvas hosting, lifecycle integration,
// UI components) begins in a later roadmap phase. Angular dependencies are added
// then. Angular never drives the engine update/render loop. Depends on
// @slice/core, @slice/renderer-three, @slice/math.
export const ANGULAR_PACKAGE = '@slice/angular';
