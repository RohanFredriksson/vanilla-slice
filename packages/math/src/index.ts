// @slice/math — placeholder module.
// Implementation (vectors, matrices, quaternions) begins in a later roadmap phase.
// This package depends on nothing (see ARCHITECTURE.md).
export const MATH_PACKAGE = '@slice/math';
