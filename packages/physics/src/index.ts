// @slice/physics — placeholder module.
// Implementation (fixed-timestep integration, rigid bodies, gravity, collision,
// cleanup) begins in a later roadmap phase. Depends only on @slice/math.
export const PHYSICS_PACKAGE = '@slice/physics';
