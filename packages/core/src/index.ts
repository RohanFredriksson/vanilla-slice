// @slice/core — placeholder module.
// Implementation (ECS world, entities, components, systems, createWorld/spawn)
// begins in a later roadmap phase. Orchestrates the core packages; it never
// depends on rendering adapters or framework integrations.
export const CORE_PACKAGE = '@slice/core';
