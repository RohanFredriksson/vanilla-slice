// apps/portfolio — placeholder entry point.
// This app CONSUMES the engine. Implementation begins in a later roadmap phase.
// The render/update loop is imperative and owned by the app, never by a
// framework's change detection (see ADR 0005).
export const PORTFOLIO_APP = '@slice/portfolio';
